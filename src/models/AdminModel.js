import UsuarioModel from "./UsuarioModel";

export default class AdminModel extends UsuarioModel {
    constructor(data) {
        super(data);
        if(data==null) {
            
        } else {
        }
        
    }
}